# WORKLOG — Thanks Project

> **Mọi người (và mọi Claude Code session) đều phải đọc file này trước khi bắt đầu làm bất cứ việc gì.**  
> Mục đích: tránh 2 người làm trùng nhau, biết context của nhau để làm tiếp đúng chỗ.

---

## Cách dùng

**Trước khi bắt đầu:**
1. Đọc toàn bộ file này
2. Tìm task mình sẽ làm trong Backlog
3. Di chuyển task đó sang **In Progress**, điền tên + ngày

**Khi làm xong:**
1. Di chuyển task sang **Done**
2. Điền commit hash hoặc PR link
3. Commit file WORKLOG.md này lên repo `thanks`

---

## 🔄 In Progress

| Task | Ai làm | Bắt đầu | Ghi chú |
|------|--------|---------|---------|

---

## 📋 Backlog

> **Backlog đầy đủ đã chuyển sang [`TASKS.md`](TASKS.md)** — chia theo phase, người phụ trách, và dependencies.
> File này chỉ track **In Progress** và **Done**.

---

## ✅ Done

| Task | Ai làm | Ngày xong | Commit / PR |
|------|--------|-----------|-------------|
| F8-1 + F8-2 + F8-3 + F8-8 + F8-9 — Sync Unsplash photo IDs: item.dart mock data (8 items, match prototype 2_1_1) + dev_seed.sql business logos (Bento Cooky / Gam Coffee / Tous Les Jours) + post_images (17 entries, picsum → Unsplash) | Claude | 2026-05-02 | TBD |
| F-extra-1 + F-extra-2 — Giver profile sections (posts + ratings) + bottom nav bar on all sub-screens | TrungVT (sonnet exec) | 2026-04-29 | [44ae6a4](https://github.com/thanks-org/thanks-app/commit/44ae6a4) |
| B-extra-1 + B-extra-2 — `GET /users/:id/posts` + `GET /users/:id/ratings` cho Giver Public Profile screen. Keyset cursor pagination (base64 created_at), status filter, rater+post join, aggregate summary. Docs updated in api_doc.html. | TrungVT (sonnet exec) | 2026-04-29 | [5a22e03](https://github.com/thanks-org/thanks-backend/commit/5a22e03) |
| Hotfix F-bugfix-3 — "Xem tất cả →" Givers screen mất bottom nav (Navigator.push thay vì switch tab) | TrungVT (sonnet exec) | 2026-04-29 | [0589d76](https://github.com/thanks-org/thanks-app/commit/0589d76) |
| Hotfix F-bugfix-2 — Top Givers section: card onTap + "Xem tất cả →" + real leaderboard API (was using mockTopGivers) | TrungVT (sonnet exec) | 2026-04-29 | [ad8f416](https://github.com/thanks-org/thanks-app/commit/ad8f416) |
| Hotfix F-bugfix-1 — Claim Confirmed Container negative-margin assertion crash (header rendered green, body all red ErrorWidget on iOS device) — root cause: `_PickupCodeBlock` Container had literal `margin: EdgeInsets.fromLTRB(18, -18, 18, 0)` triggering Flutter's `margin.isNonNegative` assertion; fix: replaced negative top margin with `Transform.translate(offset: Offset(0, -18))` wrapping the Container | TrungVT (sonnet exec) | 2026-04-29 | [5bed0b0](https://github.com/thanks-org/thanks-app/commit/5bed0b0) |
| Hotfix B-bugfix-1 — `GET /me` crash NULL phone scan (`repository/users.go` GetMe) — discovered during wave 2 QA via dev user; fix `*string` + `derefString` pattern (audit nearby scans too) | TrungVT (sonnet exec) | 2026-04-29 | [24f9ac8](https://github.com/thanks-org/thanks-backend/commit/24f9ac8) |
| F5-5 + F5-11 — Notifications inbox screen + bottom-nav badges (notifications + messages) thật | TrungVT (sonnet exec) | 2026-04-29 | [c1873da](https://github.com/thanks-org/thanks-app/commit/c1873da) |
| F5-6 + F5-9 — Home Feed search bar (`q` debounce) + sort tabs wire vào B7-2 | TrungVT (sonnet exec) | 2026-04-29 | [441678d](https://github.com/thanks-org/thanks-app/commit/441678d) |
| Go backend skeleton + DB migrations (users, posts, claims, messages, ratings) | Claude Code | 2026-04-27 | [c84fcab](https://github.com/thanks-org/thanks-backend/commit/c84fcab) |
| Docker Compose + PostgreSQL setup | Claude Code | 2026-04-27 | [b8d1a8d](https://github.com/thanks-org/thanks-infra/commit/b8d1a8d) |
| Flutter app skeleton + Home Feed screen (mock data) | Claude Code | 2026-04-27 | [865bba2](https://github.com/thanks-org/thanks-app/commit/865bba2) |
| Prototype → Screen descriptions (thanks_screens.md) | Claude Code | 2026-04-27 | trong repo `thanks` |
| Screen descriptions → API doc HTML (api_doc.html) | Claude Code | 2026-04-27 | [ad17d6c](https://github.com/thanks-org/thanks/commit/ad17d6c) |
| Tách 26 prototype screens → static HTML (idea_to_static_html/) | Claude Code | 2026-04-27 | [7d81d2e](https://github.com/thanks-org/thanks/commit/7d81d2e) |
| App icon — thanks! branding (cream, terracotta, Plus Jakarta Sans 800) | Claude Code | 2026-04-28 | [0f8fbf8](https://github.com/thanks-org/thanks-app/commit/0f8fbf8) |
| Dockerize backend — Dockerfile.dev + air hot-reload, migrate tự chạy khi `make up` | Claude Code | 2026-04-28 | [831b6a1](https://github.com/thanks-org/thanks-infra/commit/831b6a1) |
| Update README (thanks, thanks-infra, thanks-backend) phản ánh setup mới | Claude Code | 2026-04-28 | [3c3c006](https://github.com/thanks-org/thanks-infra/commit/3c3c006) |
| Tái cấu trúc docs → api_and_doc/ (API doc + DB schema diagram) | Claude Code | 2026-04-28 | [ad17d6c](https://github.com/thanks-org/thanks/commit/ad17d6c) |
| Home Feed rebuild — match prototype (màu sắc, layout, item cards, top givers, sort bar) | Claude Code | 2026-04-28 | [6893534](https://github.com/thanks-org/thanks-app/commit/6893534) |
| Fix item card overflow + đổi tên app thành "Thanks" | Claude Code | 2026-04-28 | [41503ce](https://github.com/thanks-org/thanks-app/commit/41503ce) |
| Schema extension v2 — bổ sung businesses, organizations, auth_providers, sessions, device_tokens, notifications, thanks, post_schedules + extend users/posts/claims/ratings/messages cho khớp 26 prototype screens | Claude Code (vanlang) | 2026-04-28 | [PR #3](https://github.com/thanks-org/thanks-backend/pull/3) |
| B2-1: `GET /api/v1/posts` — feed API (category filter, Haversine proximity, pagination) | Luân | 2026-04-28 | [d39d561](https://github.com/thanks-org/thanks-backend/commit/d39d561) |
| B2-2: `GET /api/v1/posts/:id` — post detail (description, address, giver profile card) | Luân | 2026-04-28 | [46578f4](https://github.com/thanks-org/thanks-backend/commit/46578f4) |
| B1-1 — JWT bearer middleware (`auth.NewIssuer`, `middleware.RequireAuth`, 11 unit tests; blocker cho toàn bộ B-* / F-* Phase 1-3) | TrungVT | 2026-04-28 | [cf01e25](https://github.com/thanks-org/thanks-backend/commit/cf01e25) |
| F1-1: API client (dio, interceptors, error types) + F1-5: Home Feed → real API | Luân | 2026-04-28 | [426fb6d](https://github.com/thanks-org/thanks-app/commit/426fb6d) |
| F1-6: Screen 2.4.1 — Item Detail (hero image, quantity bar, pickup info, giver card, sticky claim button via Scaffold.bottomNavigationBar) | Luân | 2026-04-28 | [4dd3490](https://github.com/thanks-org/thanks-app/commit/4dd3490) |
| B4-1 — `POST /uploads` (LocalStorage tạm; Storage interface để swap R2/S3 khi I1-1 xong; 10 MB cap, MIME whitelist, path-traversal hardened) | TrungVT | 2026-04-28 | [2a57a1c](https://github.com/thanks-org/thanks-backend/commit/2a57a1c) |
| B4-2..B4-6 — Phase 2 backend writer endpoints: POST /posts, PUT /posts/:id, DELETE /posts/:id, GET /posts/:id/claimants, GET /me/posts (owner checks, sentinel→HTTP error mapping, atomic image replacement, status filter) | TrungVT | 2026-04-28 | [a73c055](https://github.com/thanks-org/thanks-backend/commit/a73c055) |
| B1-4 — `POST /auth/logout` — JWT denylist (revoked_tokens table, jti+exp keyed; middleware rejects revoked tokens with 401 token_revoked; idempotent insert) | TrungVT | 2026-04-28 | [20c376d](https://github.com/thanks-org/thanks-backend/commit/20c376d) |
| B6-1 — `POST /auth/social` (Google tokeninfo + Apple JWKS/RS256; migration phone nullable; kéo lên Phase 1) | Luân | 2026-04-28 | [fa67ab9](https://github.com/thanks-org/thanks-backend/commit/fa67ab9) |
| B5-1..B5-10 — Phase 3 backend (10 endpoints): GET/PUT /me, /me/claims, /me/impact, /users/:id, /leaderboard, /messages list+thread+send, POST /claims/:id/ratings (claimParties.peerOf authz, in-TX rating_avg recompute, UNIQUE→409 on duplicate, notification row written cho FCM I4-1 consume). Reassign từ Quang (B5-1..B5-6) + Hiếu (B5-10) sang TrungVT để unblock Frontend Phase 3. | TrungVT | 2026-04-28 | [ed382c3](https://github.com/thanks-org/thanks-backend/commit/ed382c3) |
| B6-1..B6-7 — Phase 4 backend (7 endpoints): POST /auth/social (pluggable SocialVerifier per provider; dev mode accept base64-JSON id_token, production verifier để TODO chờ provider keys; find-or-create user qua auth_providers UNIQUE), GET/POST/PUT /me/businesses + /businesses/:id, GET/POST/PUT /me/organizations + /organizations/:id (owner check → 403, status mặc định pending_review). **Lưu ý**: social-only user nhận synthetic phone `@<14 hex>` (ẩn khỏi response) vì cột `phone` NOT NULL — sẽ thay khi user link OTP thật. Reassign từ Quang (B6-2..B6-7) + Hiếu (B6-1) sang TrungVT để unblock Frontend Phase 4. | TrungVT | 2026-04-28 | [413d705](https://github.com/thanks-org/thanks-backend/commit/413d705) |
| B3-1, B3-2 — `POST /posts/:id/claims` + `DELETE /claims/:id`: TX `SELECT ... FOR UPDATE` trên posts row → check status='active', user_id != caller, quantity → INSERT claim (4-digit pickup code via crypto/rand) + UPDATE quantity_remaining + INSERT notification (type='claim_created') tất cả trong 1 TX. Cancel: receiver-only, FOR UPDATE trên claim row, atomic restore quantity_remaining. Sentinels: post_not_found/404, post_not_claimable/422, self_claim/403, insufficient_quantity/422, already_claimed/409 (UNIQUE post_id+user_id), claim_not_found/404, forbidden/403, claim_not_cancellable/409. 10-step smoke đã pass (claim → 6668 pickup code, self-claim → 403, duplicate → 409, qty 2→1, cancel → 204 + qty 1→2, double-cancel → 409). Reassign từ Hiếu sang TrungVT để unblock Frontend Luân (F1-7 Claim Confirmed). | TrungVT | 2026-04-28 | [9313e58](https://github.com/thanks-org/thanks-backend/commit/9313e58) |
| B6-1 production Google id_token verifier — replaced placeholder trong `internal/social/verifier.go` bằng tokeninfo-endpoint impl (stdlib only, không thêm dep). Validate `aud == GOOGLE_CLIENT_ID`, `iss ∈ {accounts.google.com, https://accounts.google.com}`, `exp`, non-empty `sub`. Dev mode (JWT_SECRET=dev default) vẫn short-circuit để Luân test app không cần cấu hình thêm. Google Cloud Web Client ID `650801838642-...` đã tạo (project name: "Thanks"). Wire env passthrough trong `thanks-infra/docker-compose.yml` ([8827984](https://github.com/thanks-org/thanks-infra/commit/8827984)). Còn lại Zalo/Facebook/Apple verifiers vẫn TODO chờ ops set up provider keys. | TrungVT | 2026-04-28 | [26c7533](https://github.com/thanks-org/thanks-backend/commit/26c7533) |
| F1-2 + F1-7 — `AuthService` singleton (`flutter_secure_storage` key `auth_jwt`) + dio `QueuedInterceptor` auto-attach Bearer; ApiException codes mở rộng (already_claimed/self_claim/post_not_claimable/insufficient_quantity); dev-only auto-login trong `main.dart` (kDebugMode + chưa có token → `POST /auth/social` với fake Google id_token, lưu JWT) để smoke-test claim flow trước khi F1-3/F1-4 ship. F1-7: `ClaimConfirmedScreen` match prototype (forest gradient hero, 4-digit pickup code 30px spaced, item card, pickup window mustard, location card, giver card, sticky share+directions bar), wire Item Detail `_ClaimBar` → `ClaimService.claimPost` → `pushReplacement`; Cancel claim với `showDialog` confirm → `DELETE /claims/:id`. Mapped lỗi backend sang tiếng Việt cho SnackBar. Reassign từ Luân sang TrungVT. **Caveat:** Flutter SDK chưa cài trên máy ⇒ chưa chạy `flutter analyze` — Luân chạy trước khi merge staging. | TrungVT | 2026-04-28 | [4a326cb](https://github.com/thanks-org/thanks-app/commit/4a326cb), [6cfd266](https://github.com/thanks-org/thanks-app/commit/6cfd266) |
| F1-3 + F1-4 (partial) — `ProfileScreen` rewrite từ 12-line placeholder → state-aware widget branching theo `AuthService.isAuthenticated()`: logged-out render Screen 2.1.5 (logo, value-prop, "Đăng nhập"/"Đăng ký" CTA → push F1-4); logged-in render stub với nút "Đăng xuất" (TODO F2: full Screen 2.1.4*). Refresh state pattern: `Navigator.push(...).then((_) => _checkAuth())`. F1-4 `SignUpAuthMethodScreen`: static layout match prototype (phone input + OTP button, divider "Hoặc", 4 social buttons row, terms footer). Google button: kDebugMode dùng fake id_token với sub `google-signin-{millis}` để mỗi tap tạo dev user mới → POST `/auth/social` → save token → pop về Profile (auto re-check, flip sang logged-in). !kDebugMode show "Coming soon" snackbar (TODO swap sang `google_sign_in` package khi Android/iOS Client ID có). Phone+OTP + Zalo/FB/Apple buttons disabled với "Coming soon" snackbar (block trên B1-2/B1-3 + provider keys). **Caveat:** chưa chạy `flutter analyze` (Flutter SDK không có trên máy). | TrungVT | 2026-04-28 | [72ba948](https://github.com/thanks-org/thanks-app/commit/72ba948), [2ef6901](https://github.com/thanks-org/thanks-app/commit/2ef6901) |
| F2-1 + F2-2 + F2-3 — Phase 2 frontend (3/6 tasks): F2-1 `UploadService` (multipart upload `POST /uploads` qua dio `FormData`, return `{url, path}`) + `image_picker` dep. F2-2 Screen 2.2.7 Submit Item Step 1: `SubmitItemDraft` model carries `XFile` refs through flow (upload deferred tới final submit để tránh dangling uploads on back-out), 1–5 ảnh picker grid + thumbnail preview + "remove" overlay, title (required, max 200) + description text fields. F2-3 Screen 2.2.8 Submit Item Step 2: 6-category chip grid (Food/Clothes/Furniture/Tech/Books/Other), quantity stepper, pickup days multi-select, pickup time HH:mm range picker, location address + auto-detect button (placeholder lat/lng cho Q1 HCMC vì location plugin chưa wire), close-after duration radio (3/5/7/14 days). Final submit: upload images → `POST /api/v1/posts` → `pushReplacementNamed('/')` → `postCreatedTicker.value++` để Home Feed re-fetch. Home Screen: terracotta FAB → push Step 1 + listener trên `postCreatedTicker` để auto-refresh feed sau khi post-create. Reassign từ Luân (F2-1) + Đức (F2-2, F2-3) sang TrungVT. | TrungVT | 2026-04-28 | [81ff436](https://github.com/thanks-org/thanks-app/commit/81ff436), [37d6331](https://github.com/thanks-org/thanks-app/commit/37d6331), [d65ee26](https://github.com/thanks-org/thanks-app/commit/d65ee26) |
| API contract drift fix — `POST /api/v1/posts` request body field names misaligned với api_doc spec gây newly-created posts mất lat/lng (silent JSON unmarshal failure → DB stores NULL → vanish khỏi proximity feed) + reject `category=clothes`/`tech`. **Backend** (b82877f): `CreatePostRequest`/`UpdatePostRequest` rename JSON tags `latitude`→`lat`, `longitude`→`lng`; `AllowedCategories` rename `clothing`→`clothes`, `electronics`→`tech`; handler error message updated; migration `000004_align_post_category_enum.up.sql` drop+re-add `posts_category_check` CHECK + UPDATE existing rows để local DBs không reject constraint mới. **Flutter** (154b378): `home_screen.dart` `_categoryApiMap` align với backend enum. Smoke-tested: dev social-auth → `POST /posts {category:"tech", lat:10.7769, lng:106.7009}` → 201 + `GET /posts/:id` returns latitude/longitude populated. Discovered while reproducing executor's caveat từ F2-3 ship. | TrungVT | 2026-04-28 | [b82877f](https://github.com/thanks-org/thanks-backend/commit/b82877f), [154b378](https://github.com/thanks-org/thanks-app/commit/154b378) |
| F2-4 + F2-5 + F2-6 — Phase 2 frontend (3/3 còn lại): F2-4 `MyItemsPersonalScreen` (status filter Đang mở/Đã xong/Đã hủy → `GET /me/posts?status=`, item rows với thumbnail+title+quantity+status dot, swipe-to-cancel + 3-dot menu → `DELETE /posts/:id` confirm dialog, "Xem người claim" → F2-6, empty state với "Đăng đồ" CTA, pull-to-refresh, fixed bottom "Đăng món" bar). F2-5 `MyItemsBusinessScreen` + `MyBusinessesScreen` entry point (Profile → Doanh nghiệp → list businesses → tap → per-business posts với business identity strip: logo/initial + verified badge + category + post count). F2-6 `WhoClaimedScreen` (post summary header, stats bar total/completed/pending, claimant rows với avatar color-by-name-hash + name + time-ago + status chip + 4-digit pickup code). New models `claimant.dart`/`business.dart`, new service `business_service.dart`, extended `post_service.dart` với `listMyPosts()`/`listClaimants()`/`cancelPost()`. Profile logged-in branch rewired: stub "Đăng xuất" → menu "Đồ tôi đăng" / "Doanh nghiệp" / "Đăng xuất". Reassign từ Đức (F2-4..F2-6) sang TrungVT. **API doc drift fixed**: `GET /me/businesses` doc said plain array nhưng backend trả `{total, businesses}` envelope; updated api_doc.html để match backend (envelope là convention chuẩn cho list endpoints, đã dùng cho /posts/:id/claimants etc.). **Backend gap (TODO)**: `GET /me/posts?business_id=...` chưa được handler implement — Flutter pass param forward-compat nhưng server hiện trả tất cả posts của user (không filter theo business). Cần follow-up: thêm `business_id` filter vào `handler.ListMyPosts` + verify owner_user_id match (user phải own business mới filter được). **Caveat:** chưa chạy `flutter analyze` (Flutter SDK không có trên máy). | TrungVT | 2026-04-28 | [841a70c](https://github.com/thanks-org/thanks-app/commit/841a70c) |
| B4-7 + B5-11 + B5-12 + B6-8 + B6-9 — 5 backend gap fixes (3 thực sự fix, 2 đã có sẵn): **B4-7** `GET /me/posts?business_id=` filter — UUID validate (400 `invalid_business_id`), pre-flight ownership check `SELECT 1 FROM businesses WHERE id=$1 AND owner_user_id=$2` (403 `forbidden` nếu không own), thêm `business_id=$N` vào WHERE clause của ListMyPosts query. **B6-8** public `GET /businesses/:id` — handler + repo mới, no auth, 404 `business_not_found` nếu not found. **B6-9** public `GET /organizations/:id` — same pattern, 404 `organization_not_found`. **B5-11 + B5-12 đã có sẵn**: verify khi đọc code — `repository/users.go:35,192` SELECT đã include `avatar_url, bio`; `repository/messages.go:103,209` SELECT `peer.avatar_url`; models `profile.go`/`messages.go` đã có fields. Có thể fix sẵn trong ed382c3 (Phase 3 commit) hoặc commit follow-up không track — gap report stale. **B6-9 caveat**: `address_detail` field deferred — cột không tồn tại trong `organizations` schema (`migrations/000002` chỉ có `address`, không có `address_detail`). api_doc.html sai chứ không phải backend. Cần migration `ALTER TABLE organizations ADD COLUMN address_detail TEXT` + update api_doc nếu thực sự cần — task riêng. **Smoke tested**: B4-7 own→200, other→403, bad UUID→400; B6-8 valid→200/non-existent→404; B6-9 valid→200. **⚠ Migration collision phát hiện**: cả `000004_align_post_category_enum.*.sql` và `000004_phone_nullable.*.sql` cùng số 000004 — golang-migrate sẽ chỉ chạy 1 file theo alphabet, file kia bị skip silently. Cần rename 1 file thành `000005_*` (cần Luân/team coordinate vì có thể đã chạy 1 trong 2 trên staging DB). Delegated impl to sonnet executor. | TrungVT | 2026-04-28 | [790a602](https://github.com/thanks-org/thanks-backend/commit/790a602) |
| F4-1..F4-9 — Phase 4 frontend (9/9, gộp 1 commit): F4-1 polish auth screen (`SignUpAuthMethodScreen`): success toast trên kDebugMode Google login, prefix backend error message khi login fail, "Coming soon" SnackBar có context (Zalo cần Open API App ID, FB cần App ID+Secret, Apple cần Services ID — không phải plain "Coming soon" như trước). F4-2 `GiverPublicBusinessScreen` (public business view: logo+verified badge, category chip, rating, member-since, address/phone/description, "items active" count). F4-3 + F4-6 reuse `PublicProfileScreen` (F3-3) — layout prototype `2_2_2_giver_public_personal.html` + `2_3_1_receiver_public_personal.html` so với screen F3-3 đã build → equivalent (avatar, stats row, activity card, role badge), không build duplicate. F4-4 `ManageBusinessesScreen` thay `MyBusinessesScreen` từ F2-5: list businesses với verification status badge (approved=green, pending_review=mustard, rejected=terracotta), tap edit → F4-5 với prefilled data, "+ Thêm doanh nghiệp" CTA. F4-5 `AddBusinessScreen` create/edit form: name (required), category chips (food/clothes/books/tech/furniture/other), logo upload qua `image_picker`→`UploadService`→`PUT /businesses/:id` patch, address (city dropdown HCM/HN/...), phone, description, address_detail. `POST /businesses` create / `PUT /businesses/:id` edit. F4-7 `ReceiverPublicOrganizationScreen` mirror F4-2 cho org. F4-8 `ManageOrganizationsScreen` mirror F4-4. F4-9 `AddOrganizationScreen` mirror F4-5 nhưng `POST/PUT /organizations`. **ProfileScreen rewire:** Businesses + Organizations sections luôn hiển thị (không hide khi list rỗng), route `ManageBusinessesScreen` / `ManageOrganizationsScreen`. New services `organization_service.dart` + extended `business_service.dart` (`createBusiness`/`updateBusiness`). New models `organization.dart`. **API doc drift findings (cần update doc):** (1) `GET /users/:id` doc lines ~437-438 implies `businesses[]`/`organizations[]` arrays embedded — backend KHÔNG embed (chỉ trả user fields + total_posts). F4-2/F4-7 phải nhận `BusinessModel`/`OrganizationModel` từ caller. (2) Cần thêm public `GET /businesses/:id` + `GET /organizations/:id` endpoints để các màn public profile có thể standalone link (hiện chỉ access được qua Manage* → tap mình tự sở hữu). **Backend gaps tìm được (chưa fix):** (a) chưa có public `GET /businesses/:id` / `GET /organizations/:id` → F4-2/F4-7 chỉ dùng được khi caller có sẵn model. (b) `OrganizationModel` thiếu `address_detail` field (api_doc có nhưng backend chưa serialize). (c) `/users/:id` chưa embed businesses[]/organizations[] (xem #1). **F4-1 chưa hoàn chỉnh:** đánh dấu `[~]` chứ không `[x]` vì native SDK integration (Zalo Open API, Google Sign-In package thay thế fake id_token, FB Login, Apple Sign-In) vẫn block trên provider keys/credentials. Reassign từ Đức (F4-2..F4-9) + Luân (F4-1) sang TrungVT. **Caveat:** chưa chạy `flutter analyze` (Flutter SDK không có trên máy). | TrungVT | 2026-04-28 | [112ae3a](https://github.com/thanks-org/thanks-app/commit/112ae3a) |
| F3-1..F3-8 — Phase 3 frontend (8/8): F3-1/F3-2 ProfileScreen rewrite logged-in branch dùng `GET /me` (avatar/name/joined/stats/edit pencil); auto-switch personal↔with-business dựa trên `listMyBusinesses()` (1 màn hình thay vì 2). F3-3 `PublicProfileScreen` đọc `/users/:id` (read-only), wired từ Who's Claimed claimant rows + Leaderboard rows (replace "coming soon" snackbar). F3-4 `SettingsScreen` (avatar upload qua image_picker→UploadService→`PUT /me`, name/bio editing, read-only phone/email, notif toggles local-state, sign-out với confirm). F3-5 `ThanksRatingsScreen` (`GET /me/impact` — total_posts_completed, total_items_given, rating stats; recent_thanks list — empty đến khi có completed claims). F3-6 `GiversScreen` rewrite (top-3 podium + list, period tabs week/month/all-time, tap row → F3-3). F3-7+F3-8 `MessagesScreen` rewrite (conversation list từ `GET /messages` — đã gộp 2 task vì cùng endpoint), `MessageThreadScreen` mới (bubble UI sent-right/received-left, composer, `POST /messages/claims/:claim_id`); rating dialog khi `claim_status='completed'` → `POST /claims/:id/ratings`. New services `me_service.dart`/`messages_service.dart`/`leaderboard_service.dart`/`rating_service.dart`; new models `user_profile.dart`/`me_impact.dart`/`leaderboard_entry.dart`/`conversation.dart`. **API doc drift findings (cần update doc sau):** `/me` thực tế trả `{id,phone,email,name,role_type,is_verified,rating_avg,rating_count,created_at,stats{total_posts,total_claims,total_thanks_received},identity_list[]}` — doc chưa specify đầy đủ. `/users/:id` trả thêm `total_posts` (không có trong doc). `/leaderboard` filter theo `completed`/`picked_up` claims (doc cần note). `/messages` không có `last_message` field như implied. **Backend gaps tìm được (chưa fix):** (a) `avatar_url` vắng mặt trong response của `/me`, `/users/:id`, `/messages` (peer.avatar_url), `/messages/claims/:id` (peer.avatar_url) — cột tồn tại trong schema users nhưng repository SELECT chưa project. (b) `/messages/claims/:id` không trả `claim_status` trong thread response (đang lấy từ conversation list rồi pass qua). (c) `bio` field nhận được khi `PUT /me` nhưng không trả về trong `GET /me` — text field Settings sẽ luôn empty on reload. UI fallback: avatar dùng initials với background color-by-name-hash, hoạt động fine cho dev. Reassign từ Đức (F3-1..F3-3, F3-5, F3-6) + Luân (F3-4) sang TrungVT (vốn đã được assign F3-7/F3-8). Reasoning: Đức/Luân chưa start → tao cover Phase 3 cho team. **Caveat:** chưa chạy `flutter analyze` (Flutter SDK không có trên máy). | TrungVT | 2026-04-28 | [42af304](https://github.com/thanks-org/thanks-app/commit/42af304) |
| F5-1..F5-4 — Phase 5 P0 frontend wiring (4/4): F5-1 Claim Confirmed → "Nhắn tin" nav `MessageThreadScreen` với `Conversation` dựng từ `ClaimResponse` + `postId` (peer.id để rỗng, fallback ổn vì `_myUserId` từ JWT là source of truth cho bubble direction). F5-2 "Chỉ đường" gọi `url_launcher` với address — try `geo:0,0?q=<encoded>` trước (bật native maps trên Android/iOS), fallback `https://www.google.com/maps/search/?api=1&query=<encoded>` nếu không có app maps. F5-3 Item Detail `_HeroImage` refactor `StatelessWidget` → `Stateful` với `PageView.builder` qua `post.images[]`, dot indicator (active=18×6 white, inactive=6×6 white55%), counter pill `1/N` góc top-right. F5-4 `_PickupCodeBlock` thêm `GestureDetector` onTap+onLongPress → `Clipboard.setData` + SnackBar "Đã sao chép mã"; subtitle update "Chạm để sao chép — đưa hoặc đọc mã này...". Add `url_launcher: ^6.3.1` (auto-pulled v6.3.2). `flutter analyze` clean (chỉ 6 info-level `unnecessary_underscores` lint pre-existing). Phase 5 còn F5-5..F5-12 (P1/P2 UX polish) + B7-1..B7-3 (notifications/search/read endpoints) chưa claim. | TrungVT | 2026-04-29 | [925cca4](https://github.com/thanks-org/thanks-app/commit/925cca4) |
| B7-1 + B7-2 + B7-3 — Phase 5 backend P1 (3/3): **B7-1** `GET /api/v1/me/notifications?limit&offset&unread_only` — pagination (limit cap 50, default 20), response `{total, unread_count, notifications[]}`. New: `model/notification.go`, `repository/notifications.go`, `handler/notifications.go`. **Schema discovery**: `notifications` table có `id, user_id, type, related_entity_id, title, body, is_read, created_at` — KHÔNG có `data` JSONB như api_doc giả định ⇒ response omit field; api_doc cần update. **B7-2** mở rộng `GET /api/v1/posts` thêm `q` (ILIKE escape `% _ \`) + `sort=recent\|expiring\|distance`, default = distance khi có lat/lng else recent, fallback `sort=distance` không có lat/lng silent → recent. Zero impact callers cũ. **B7-3** `POST /api/v1/me/notifications/read` body `{ids:[...]}` hoặc `{all:true}` → `{updated:N}`. UPDATE filter `WHERE user_id=$1 AND id = ANY($2)`. Delegated cho sonnet executor. Smoke test chưa run (Go binary không trên PATH cho agent shell, docker chưa start) — sẽ verify khi Railway deploy hoặc local docker up. | TrungVT (sonnet exec) | 2026-04-29 | [0615c43](https://github.com/thanks-org/thanks-backend/commit/0615c43) |
| Dev tooling — `setup.sh` rewrite (`./setup.sh [dev\|prod]`: git pull 3 repos, auto-start Docker Desktop, docker compose up, wait DB + migrate, seed dev/prod, start iOS Simulator nếu chưa chạy, flutter run); rename `seed-fake`→`seed-dev` / `seed-normal`→`seed-prod` trong Makefile + CLAUDE.md + settings; tạo `pull.sh` (pull 3 repos) + `status.sh` (check unpushed/uncommitted across root + 3 sub-repos) | Claude Code | 2026-04-30 | (commit này) |
| B9-1 + B9-2 — `POST /claims/:id/confirm-pickup` (giver verify pickup code → picked_up; owner check via JOIN posts, wrong-code 422, notification) + `POST /claims/:id/confirm` (pending → confirmed; giver only, notification to receiver). New error sentinels + model types. F7-10 WhoClaimedScreen: confirm/reject buttons cho pending claims + "Xác nhận nhận hàng" cho confirmed. F7-1 PickupConfirmationScreen mới: 4-digit input, paste support, success state. `flutter analyze` clean. | Claude Code (sonnet exec) | 2026-04-30 | backend [634fae0](https://github.com/thanks-org/thanks-backend/commit/634fae0), flutter [77a98f0](https://github.com/thanks-org/thanks-app/commit/77a98f0) |
| F5-7 + F5-12 — Phase 5 frontend P1 (2/2 độc lập): **F5-7** `home_screen.dart` wrap `RefreshIndicator` (terracotta) + `ScrollController` listener trigger `_loadMorePosts()` khi cách 300px from bottom, page size 20, append `_items`, stop khi API trả < limit, loading row cuối + error row "Tải thêm thất bại — chạm để thử lại"; category filter pass-through mỗi page. **F5-12** thêm `share_plus: ^10.1.0` (resolved 10.1.4), thay SnackBar stub bằng `Share.share(text, sharePositionOrigin: ...)`; share text VN gồm title + 4-digit pickup code + (optional) pickup window + address (drop dòng trống); iPad sharePositionOrigin fallback screen center. Delegated cho sonnet executor. `flutter analyze` clean. Manual device test chưa run (no simulator active). | TrungVT (sonnet exec) | 2026-04-29 | [4b1ebdb](https://github.com/thanks-org/thanks-app/commit/4b1ebdb) |
